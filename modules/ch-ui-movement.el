;;; ch-ui-movement.el --- UI movement stuff
;;; Commentary:
;;; Code:
(require 'windmove)
(windmove-default-keybindings)

;(add-hook 'focus-out-hook 'save-buffer)

(provide 'ch-ui-movement)
;;; ch-ui-movement.el ends here
